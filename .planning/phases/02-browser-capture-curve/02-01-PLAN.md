---
phase: 02-browser-capture-curve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/02-browser-capture-curve/02-CONTEXT.md
autonomous: true
requirements:
  - AUD-01
  - AUD-02
  - AUD-03
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - "Phase 2 is described as backend-owned microphone capture with browser-only visualization."
    - "Phase 2 requirements no longer depend on browser getUserMedia ownership."
    - "Project state clearly shows the old browser-capture prototype is being replaced by a backend-capture refactor."
  artifacts:
    - ".planning/PROJECT.md"
    - ".planning/REQUIREMENTS.md"
    - ".planning/ROADMAP.md"
    - ".planning/STATE.md"
    - ".planning/phases/02-browser-capture-curve/02-CONTEXT.md"
  key_links:
    - "ROADMAP.md Phase 2 goal, REQUIREMENTS.md AUD/VIS entries, and 02-CONTEXT.md all describe the same backend-capture behavior."
    - "STATE.md current focus points execute-phase at the backend-capture refactor rather than the already-built browser prototype."
---

<objective>
Align project planning artifacts with the new Phase 2 decision: backend owns microphone capture and the browser becomes a passive display page.

Purpose: Keep execution, verification, and later roadmap reviews aimed at the refactor the user actually wants instead of the already-complete browser-capture prototype.
Output: Updated planning docs and truthful Phase 2 statuses that match the backend-capture direction.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-browser-capture-curve/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the phase contract around backend capture</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/02-browser-capture-curve/02-CONTEXT.md</files>
  <read_first>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/02-browser-capture-curve/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `.planning/ROADMAP.md` so Phase 2 is named `Backend Capture & Dashboard`, its goal states that the local backend directly samples the system default microphone, and its success criteria explicitly say the browser does not request microphone permission and only renders backend-provided data. Update `.planning/PROJECT.md` so "What This Is", "Core Value", and the active requirement bullets describe backend-owned sampling plus browser-only visualization. Update `.planning/phases/02-browser-capture-curve/02-CONTEXT.md` to keep the same file path but use the same renamed phase title and wording, while preserving the previously locked decisions.</action>
  <verify>rg -n "Backend Capture & Dashboard|后端直接采样系统默认麦克风|浏览器不再申请麦克风权限" .planning/ROADMAP.md .planning/PROJECT.md .planning/phases/02-browser-capture-curve/02-CONTEXT.md</verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `### Phase 2: Backend Capture & Dashboard`
    - `.planning/ROADMAP.md` contains `浏览器不再申请麦克风权限`
    - `.planning/PROJECT.md` contains `浏览器只负责展示`
    - `.planning/phases/02-browser-capture-curve/02-CONTEXT.md` contains `# Phase 2: Backend Capture & Dashboard - Context`
  </acceptance_criteria>
  <done>Phase 2 contract reads consistently as backend capture plus browser-only dashboard in roadmap, project, and context files.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline Phase 2 requirements and state for the refactor</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/02-browser-capture-curve/02-CONTEXT.md</read_first>
  <action>Rewrite `AUD-01`, `AUD-02`, and `AUD-03` in `.planning/REQUIREMENTS.md` so they describe backend sampling, backend stop/error handling, and backend-produced `dBFS` data instead of browser `getUserMedia` ownership. Rewrite `VIS-01` and `VIS-02` so the browser renders a backend-fed live curve and status-only dashboard. Change the Phase 2 traceability rows for `AUD-01`, `AUD-02`, `AUD-03`, `VIS-01`, and `VIS-02` from `Complete` back to `Pending` because the new direction replaces the old implementation. Update `.planning/STATE.md` so current focus and next steps explicitly say Phase 2 is queued for execute-phase under the backend-capture refactor.</action>
  <verify>rg -n "本地后端服务可以使用系统默认麦克风开始采样|浏览器可以展示后端提供的最近 5 分钟分贝时间曲线|Phase 2.*Pending|等待 execute-phase 按新方案重构" .planning/REQUIREMENTS.md .planning/STATE.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `**AUD-01**: 本地后端服务可以使用系统默认麦克风开始采样`
    - `.planning/REQUIREMENTS.md` does not contain `网页中点击开始监听`
    - `.planning/REQUIREMENTS.md` contains `| AUD-01 | Phase 2 | Pending |`
    - `.planning/REQUIREMENTS.md` contains `| VIS-02 | Phase 2 | Pending |`
    - `.planning/STATE.md` contains `等待 execute-phase 按新方案重构`
  </acceptance_criteria>
  <done>Requirements and state truthfully reflect that Phase 2 is being reworked from browser capture to backend capture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Backend Capture & Dashboard|后端直接采样系统默认麦克风|浏览器只负责展示" .planning/PROJECT.md .planning/ROADMAP.md .planning/phases/02-browser-capture-curve/02-CONTEXT.md` returns matches
- [ ] `rg -n "Pending" .planning/REQUIREMENTS.md` shows Phase 2 requirement rows reset from complete status
- [ ] `.planning/STATE.md` clearly points the next action to execute-phase for the backend-capture refactor
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No planning artifact still describes browser-owned microphone capture for Phase 2
- Phase 2 requirement/status truth is restored before code changes begin
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-capture-curve/02-01-SUMMARY.md`
</output>
