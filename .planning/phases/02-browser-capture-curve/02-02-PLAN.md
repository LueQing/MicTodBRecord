---
phase: 02-browser-capture-curve
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - package.json
  - src/app-server.js
  - src/mic-source.js
  - tests/app-server.test.js
autonomous: true
requirements:
  - AUD-01
  - AUD-03
  - VIS-01
  - VIS-02
user_setup:
  - "If `npm install naudiodon` fails on Windows, install the C++ build tools needed by node-gyp, then rerun install."
must_haves:
  truths:
    - "The local backend can sample the system default input device without browser participation."
    - "The backend publishes live sample, timeline, and capture-status data that the browser can consume directly."
    - "Automated tests can exercise the live-data path without requiring a physical microphone."
  artifacts:
    - "package.json"
    - "src/mic-source.js"
    - "src/app-server.js"
    - "tests/app-server.test.js"
  key_links:
    - "`src/mic-source.js` feeds normalized dBFS samples into `src/app-server.js` without using `/api/readings` as a browser upload path."
    - "`src/app-server.js` exposes a browser-readable live stream endpoint backed by the same samples used for rolling stats and TCP broadcast."
    - "`tests/app-server.test.js` injects a fake sample source into `createDbMonitorApp` so the pipeline is verified without real hardware."
---

<objective>
Move microphone ownership into the backend and expose live data for the passive dashboard.

Purpose: Replace the browser upload loop with a single backend-owned capture pipeline so rolling stats, TCP broadcast, and the future browser dashboard all read from the same source of truth.
Output: A backend microphone adapter, server-side live-stream endpoint, and automated tests for the new data path.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-browser-capture-curve/02-CONTEXT.md
@package.json
@src/app-server.js
@public/app.js
@tests/app-server.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a backend microphone adapter behind a testable interface</name>
  <files>package.json, src/mic-source.js, src/app-server.js</files>
  <read_first>package.json, src/app-server.js, .planning/phases/02-browser-capture-curve/02-CONTEXT.md</read_first>
  <action>Add `naudiodon` to `package.json`. Create `src/mic-source.js` that exports `createMicSource({ onSample, onStatus })`. Inside that module, use `require("naudiodon")`, resolve the default input device from `portAudio.getHostAPIs().defaultHostAPI` plus `portAudio.getDevices()`, and create `new portAudio.AudioIO({ inOptions: { channelCount: 1, sampleFormat: portAudio.SampleFormat16Bit, sampleRate: defaultInput.defaultSampleRate, deviceId: defaultInputId, closeOnError: true } })`. Convert 16-bit PCM chunks to RMS and then to `dBFS`, emitting `{ db, timestamp }` via `onSample`. Emit status payloads like `{ state: "live" | "unavailable" | "error", deviceName, message }` via `onStatus`. If there is no default input device, report `state: "unavailable"` instead of throwing at startup.</action>
  <verify>rg -n "naudiodon|createMicSource|SampleFormat16Bit|defaultInput|state: \"unavailable\"" package.json src/mic-source.js src/app-server.js</verify>
  <acceptance_criteria>
    - `package.json` contains `"naudiodon"`
    - `src/mic-source.js` contains `createMicSource`
    - `src/mic-source.js` contains `portAudio.AudioIO`
    - `src/mic-source.js` contains `SampleFormat16Bit`
    - `src/mic-source.js` contains `state: "unavailable"` or equivalent unavailable-status literal
  </acceptance_criteria>
  <done>The codebase has a dedicated backend microphone adapter that can sample the default input device and report capture state without relying on the browser.</done>
</task>

<task type="auto">
  <name>Task 2: Replace browser uploads with backend-fed live stream and timeline state</name>
  <files>src/app-server.js, tests/app-server.test.js</files>
  <read_first>src/app-server.js, tests/app-server.test.js, src/mic-source.js, public/app.js</read_first>
  <action>Refactor `createDbMonitorApp` in `src/app-server.js` so it accepts an optional `sampleSourceFactory` dependency that defaults to `createMicSource`. Start that source when the app starts, push every sample directly through `addReading(db, timestamp)`, and maintain a rolling timeline array for the same 5-minute window. Add `GET /api/live` as a Server-Sent Events endpoint that sends `event: status` payloads and `event: sample` payloads containing `{ db, timestamp, stats }`. Keep `GET /api/stats`, but change `POST /api/readings` to return `410` with a message like `Browser uploads are disabled; backend capture is the only live source.` Update `tests/app-server.test.js` to inject a fake `sampleSourceFactory` with deterministic samples/status events, assert that `/api/live` emits SSE frames, and keep validating rolling-window expiry plus TCP broadcast from the same backend-fed samples.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/app-server.js` contains `text/event-stream`
    - `src/app-server.js` contains `/api/live`
    - `src/app-server.js` contains `410`
    - `tests/app-server.test.js` contains `sampleSourceFactory`
    - `npm test` exits 0
  </acceptance_criteria>
  <done>The backend is the single live source for stats and timeline data, and automated tests verify the SSE plus TCP pipeline without a real microphone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "/api/live|text/event-stream|Browser uploads are disabled" src/app-server.js` returns matches
- [ ] `rg -n "naudiodon|createMicSource|SampleFormat16Bit" package.json src/mic-source.js` returns matches
- [ ] `npm test` passes with the fake sample source path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Backend capture is decoupled from browser code and wrapped behind a replaceable adapter
- Live timeline data is emitted directly from the backend for the future passive dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-capture-curve/02-02-SUMMARY.md`
</output>
