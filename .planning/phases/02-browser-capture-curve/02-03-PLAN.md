---
phase: 02-browser-capture-curve
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - README.md
  - public/app.js
  - public/index.html
  - public/styles.css
autonomous: false
requirements:
  - AUD-02
  - VIS-01
  - VIS-02
user_setup:
  - "Run `npm start`, open http://127.0.0.1:3000, and verify the passive dashboard against a real microphone after code changes land."
must_haves:
  truths:
    - "The browser never asks for microphone permission because it no longer captures audio."
    - "The dashboard renders the 5-minute curve and status entirely from backend-fed live data."
    - "The UI communicates backend availability clearly when no microphone is present or capture fails."
  artifacts:
    - "public/index.html"
    - "public/app.js"
    - "public/styles.css"
    - "README.md"
  key_links:
    - "`public/app.js` consumes `/api/live` instead of calling `getUserMedia` or posting to `/api/readings`."
    - "`public/index.html` and `README.md` both describe the browser as a display-only page."
    - "Human verification confirms the passive dashboard updates when the backend mic source is live."
---

<objective>
Turn the website into a passive dashboard that renders backend-provided microphone data and guide the user through final hardware verification.

Purpose: Complete the refactor by removing browser capture behavior, wiring the UI to the backend live stream, and making the user-facing docs and page copy match the new architecture.
Output: A passive browser dashboard, updated usage docs, and a human verification checkpoint for the real microphone path.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
@~/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-browser-capture-curve/02-CONTEXT.md
@public/index.html
@public/app.js
@public/styles.css
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove browser capture controls and restate the UI as display-only</name>
  <files>public/index.html, public/styles.css, README.md</files>
  <read_first>public/index.html, public/styles.css, README.md, .planning/phases/02-browser-capture-curve/02-CONTEXT.md</read_first>
  <action>Update `public/index.html` so the hero text, notes, and status labels explicitly say the backend is using the system default microphone and the browser is only displaying live data. Remove the `startButton` and `stopButton` elements entirely; the page should no longer present start/stop controls. Adjust `public/styles.css` to keep the layout balanced after the actions block is removed or repurposed. Update `README.md` so the run instructions say the page should open without a microphone permission prompt and the live chart depends on backend capture, not browser capture.</action>
  <verify>rg -n "开始监听|停止|浏览器只负责展示|后端正在使用系统默认麦克风|不会请求麦克风权限" public/index.html README.md</verify>
  <acceptance_criteria>
    - `public/index.html` does not contain `startButton`
    - `public/index.html` does not contain `stopButton`
    - `public/index.html` contains `浏览器只负责展示`
    - `README.md` contains `不会请求麦克风权限`
  </acceptance_criteria>
  <done>The UI copy and docs no longer imply browser-owned audio capture or browser-side start/stop controls.</done>
</task>

<task type="auto">
  <name>Task 2: Subscribe the dashboard to the backend live stream</name>
  <files>public/app.js</files>
  <read_first>public/app.js, public/index.html, src/app-server.js</read_first>
  <action>Rewrite `public/app.js` so the page no longer imports or uses `navigator.mediaDevices.getUserMedia`, `AudioContext`, `AnalyserNode`, `sendReading`, or any button click handlers. Keep the existing chart-drawing code and timeline buffer, but feed it from `new EventSource("/api/live")`. Handle `status` events to update the status pill and empty/unavailable messages. Handle `sample` events to append `{ timestamp, db }`, update the current value, and refresh `maxDb`, `avgDb`, `sampleCount`, and `updatedAt` from the event payload. On stream errors, show a disconnected/unavailable server state and rely on EventSource reconnection instead of custom mic restart logic.</action>
  <verify>rg -n "EventSource\\(\"/api/live\"\\)|navigator\\.mediaDevices|getUserMedia|AudioContext|sendReading" public/app.js</verify>
  <acceptance_criteria>
    - `public/app.js` contains `new EventSource("/api/live")`
    - `public/app.js` does not contain `navigator.mediaDevices`
    - `public/app.js` does not contain `getUserMedia`
    - `public/app.js` does not contain `AudioContext`
    - `public/app.js` does not contain `sendReading`
  </acceptance_criteria>
  <done>The browser renders live backend-fed data without owning any microphone or upload logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Passive dashboard served from the local app, reading live data from `/api/live` while the backend samples the default microphone.</what-built>
  <how-to-verify>Run `npm start`, open `http://127.0.0.1:3000`, and verify all of the following: the page never prompts for browser microphone permission; the current dB value and curve move when the backend is sampling; the 5-minute max/avg still update; and if backend capture becomes unavailable, the page shows a clear unavailable state instead of asking the browser for mic access.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "EventSource\\(\"/api/live\"\\)|浏览器只负责展示|不会请求麦克风权限" public/app.js public/index.html README.md` returns matches
- [ ] `rg -n "navigator\\.mediaDevices|getUserMedia|AudioContext" public/app.js` returns no matches
- [ ] Human verification checkpoint is approved by the user
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Browser microphone ownership is fully removed from the UI
- The passive dashboard renders backend-fed live data and communicates capture state clearly
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-capture-curve/02-03-SUMMARY.md`
</output>
